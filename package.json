{
  "name": "wayback",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio web de WayBack - De Regreso al Camino",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "compare": "node tools/verify-html.mjs reference/index.html _site/index.html",
    "verify": "npm run build && npm run compare"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
